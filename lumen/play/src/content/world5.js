// 世界四 · 滑
// 新元素：滑行段 ':'。
//
// 这一章是整个游戏的转折点，而它加的不是能力，是**减法**。
// 前三章花了三十多个房间把一件事刻进肌肉记忆：松手就停，停下来就安全，
// 等巡逻过去是这游戏的核心动词。滑行段把那条规则拿走 —— 巡逻照走，你停不下来。
//
// 所以本章的题目形状和前面都不同：前面是"什么时候动"，这里是"进去之前想清楚"。
// 判断被迫提前到踏上滑行段的那一刻，一旦上去就只剩转向。
//
// 两条自己给自己定的红线：
//   · 滑行段上必须允许反向。否则滑进死路就是软锁 —— 这是铁律二，已经踩过。
//   · 不和推块同房。你站不住的地方顶不住方块，两种语义会互相打架。

export const WORLD5 = [

  // 01 · 滑 —— 安全展示。没有任何威胁，只让你发现"松手了它还在走"。
  //      错过下面那颗光点也不要紧，走回来就是了 —— 代价只有几步路，
  //      但"我刚才想停却停不住"这件事已经发生了
  { name: 'w5-01-slip', ascii: `
[###::::::::##+###]
              #
              o
` },

  // 02 · 择 —— 岔路口挪到滑行段正中。你没有停下来看一眼的机会，
  //      得在踏上虚线之前就决定往哪拐。上面是光点，下面是主路
  { name: 'w5-02-commit', ascii: `
      o
      #
[###:::::::#
      #
      #
      ####]
` },

  // 03 · 切 —— 滑行段竖起来了：你从上面掉下去，而巡逻横着切过来。
  //      能等的地方只有入口那个拐角 —— 一旦掉进去就只剩看准
  { name: 'w5-03-cross', ascii: `
[#####+
      :
    1:::1
      :
      :
      #####]
`, opts: { speeds: { 1: 3.2 } } },

  // 04 · 升 —— 把整条线往上带三行。世界四的房间普遍是宽扁的，
  //      这里给一段纵向的呼吸
  { name: 'w5-04-climb', ascii: `
     #####]
     #
     #
[#####
` },

  // 05 · 折 —— 整条路线是一个全滑行的 U 形，只有三个墙角能停。
  //      于是"转向"成了唯一的动词：撞到底、拐、再撞到底、再拐
  { name: 'w5-05-corner', ascii: `
[##:::::::
         :
   :::::::
   #
   ###]
` },

  // 06 · 秒 —— 计时 + 滑行。倒计时里有一个直角要拐，而拐角就在滑行段上：
  //  · 秒 —— 倒计时里夹着一个二选一：支路尽头有光点，而支路口开在虚线上。
  //      贪不贪那一颗，必须在滑进去之前就定了 —— 拐错方向就来不及回来
  { name: 'w5-06-tick', ascii: `
   A
   #
[##+#::::+:::::a##]
         :
         o
`, opts: { buttons: { A: { mode: 'timer', time: 2.6 } } } },

  // 07 · 疾 —— 加速带把你甩进虚线，然后是一个岔口：直走是死路，
  //      要拐的那一下发生在全房最快的速度上。而拐下去之后还有一道横切
  { name: 'w5-07-fast', ascii: `
[##~~~~:::+::::::#
          :
        1::::1
          :
          ####]
`, opts: { speeds: { 1: 2.4 } } },

  // 08 · 跳 —— 落点紧接着一个岔口，而落点本身也是滑行段：
  //  · 跳 —— 落点紧接着一个三岔口，而落点两侧都是虚线：
  //      从传送门里出来的那一瞬间没有思考时间，方向必须在跳进去之前选好。
  //      右边那条是死枝，滑进去就得反向退回来
  { name: 'w5-08-jump', ascii: `
[#####+
      :
      :
      P

    o:p::::
      :
      #####]
` },

  // 09 · 闩 —— 主廊到底是死路，唯一的出路是那条不能回头的上弧，
  //  · 阀 —— 一个单向的滑行环。入口只能上、环只能顺时针，出口在环的右侧半路。
  // 09 · 阀 —— 光点支路的口子开在虚线上，而虚线末端是一道单向阀。
  //      滑过头还能反向退回来重拿，但只要越过那道阀就再也回不去了 ——
  //      "什么时候拐"这件事第一次有了不可逆的截止线
  { name: 'w5-09-oneway', ascii: `
[##:::+:::::>##]
      #
      o
` },

  // 10 · 拨 —— 滑行 + 反相门。两条路各带一段滑行，开着的那条随开关切换。
  //  · 拨 —— 两个按钮驱动同一条链路，而它们在同一段虚线上：
  //      一路滑过去等于连拨两次，门又关上了。
  //  · 拨 —— 两个按钮驱动同一条链路，而它们在同一段虚线上：
  //      一路滑过去等于连拨两次，门又关上。只能在碰到第二个之前拐进中间那条支路，
  // 10 · 拨 —— 两个开关驱动同一条链路，出口那条支路夹在它们中间。
  //      只碰第一个就能走：拨开、拐下去、出门。
  //      但光点在第二个开关之后的死枝里 —— 去拿它就等于把门又拨关了，
  //      回来还得再碰一次第二个开关，而两道横切各要多穿一遍。
  //      这颗光点的代价，正好由"停不下来"和"连拨两次"共同构成
  { name: 'w5-10-swap', ascii: `
     1       2      
[##::A:::::B:::::## 
     1  :    2  :   
        a       a   
        #       o   
        ####]       
`, opts: {
    buttons: { A: { mode: 'toggle', links: 'a' }, B: { mode: 'toggle', links: 'a' } },
    speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 },
  } },

  // 11 · 滑 —— 一段很长的纯滑行走廊，没有任何威胁。
  //      这一章一直在用"停不下来"惩罚你，这里让你纯粹享受它
  { name: 'w5-11-glide', ascii: `
[###::::::::::::###]
` },

  // 12 · 链 —— 之字形：滑右、拐、滑下、拐、滑左、拐、滑下。
  //  · 链 —— 之字形的两段各有一道横切，而两个实线拐角是全房仅有的落脚点。
  //      在哪个拐角上等、等多久，是要提前排的
  { name: 'w5-12-chain', ascii: `
     1
[###::::+
     1  :
      2 :
    +::::
    :  2
    :
    ####]
`, opts: { speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 } } },

  // 13 · 井 —— 一条巡逻沿着你要走的整条路来回：先下井，再跑出口跑道。
  //      井身全是虚线，所以井里没有一个地方能停；三个壁龛是实心的，
  //      那是全房唯一能让它过去的地方 —— 深的、带光点的、只有一格的。
  //      井底是静止敌：最后一个路口不是"可以拐"，是"必须拐"。
  //      难点不在按得准，在出发前就得算好这一跳够不够到下一个壁龛。
  { name: 'w5-13-shaft', ascii: `
[###1
    :
  ##:
    :
    :#o
    :
   #:
    :  2
    :::::1]
    x  2
`, opts: { speeds: { 1: 4.2, 2: 2.2 } } },

  // 14 · 换 —— 两条弧各带一段虚线，开关一拨就换边。而进弧的口子也在虚线上：
  //      你必须在拨之前就想好走哪一条 —— 拨完再改主意，退路已经关了
  { name: 'w5-14-shift', ascii: `
        1    2
     #a::o::a####
     :  1    2  :
[####+          +##]
   # :          :
   A ##b::::#####
`, opts: {
    buttons: { A: { mode: 'toggle', links: 'ab' } }, gates: { b: 'invert' },
    speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 },
  } },

  // 15 · 窄 —— 凹槽在滑行段前面的实线上：
  //  · 窄 —— 两道横切之间只有两格实线。想分两次过，就必须精确停在那两格上；
  //      想一次冲过，就得等一个两道同时开的窗口
  { name: 'w5-15-notch', ascii: `
     1     2
[##+:::##::::+##]
   # 1     2
   o
`, opts: { speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 } } },

  // 16 · 险 —— 出口在静止敌前一格，而横切巡逻正卡在出口那一段上。
  // 16 · 险 —— 一条虚线上串了两次"必须提前拐"，而且第二次比第一次紧：
  //      第一个下口通向光点死枝，第二个下口才是出路，再往前一格就是静止敌。
  //      两道横切分别卡在第一个口子之前和两个口子之间 ——
  //      贪那颗光点意味着要多穿一次横切，而出口的窗口不会为你多等
  { name: 'w5-16-peril', ascii: `
     1    2
[##:::::::::::#x
     1 :  2 :
       o    :
            ####]
`, opts: {
    speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 },
  } },

  // 17 · 锁 —— 支路口开在虚线上，而横切巡逻就卡在口子前面一格。
  //  · 锁 —— 计时钮开的是远处那道门，而路上有两个开关钮驱动同一条支路链路。
  //      倒计时里滑过去会把支路连拨两次 —— 想顺手拿那颗光点，
  // 17 · 锁 —— 本章复杂度的第二个高点，四件事同时算：
  //      计时钮开的是尽头那道门；路上两个开关驱动同一条链路，
  //      滑过去等于连拨两次；被它们控制的那道门守着光点支路，
  //      而支路口开在两个开关之间的虚线上；横切巡逻正卡在支路口后面。
  //      顺序只有一个：按计时 → 拨第一个开关 → 在第二个之前拐下去 →
  //      避开横切拿到光点 → 上来拨第二个（此时门关不关已无所谓）→ 赶在倒计时结束前出去
  { name: 'w5-17-latch', ascii: `
   A
   #       1
[##+::B::::::B::a##]
         b 1
         #
         o
`, opts: {
    buttons: {
      A: { mode: 'timer', time: 3.4 },
      B: { mode: 'toggle', links: 'b' },
    },
    speeds: { 1: 2.4 },
  } },

  // 18 · 叠 —— 线在自己身上折了两次，你会看见刚才走过的那一段
  { name: 'w5-18-fold', ascii: `
[########
       #
  ######
  #
  ########]
` },

  // 19 · 长 —— 两条虚线长廊上下相叠，两个下口把它们连起来。
  //      出口的下口在下廊正中，而它左右各卡着一道横切 ——
  //      所以从左边那个下口过来要过左边那道，从右边过来要过右边那道：
  //      走哪条不是口味问题，是"此刻哪一道的窗口先开"。
  //      而这个决定得在上廊里提前六格做完，因为虚线上停不下来。
  //      两颗光点各自紧挨着一个下口，单拿都便宜；两颗都要，
  //      就得把下廊整条走穿，两道横切各多过一遍。
  { name: 'w5-19-long', ascii: `
[##:::::::::::::#
      :      :
      : 1   2:
   o::::::::::::o
        1 : 2
          ########]
`, opts: { speeds: { 1: 2.4, 2: 3.4 } } },

  // 20 · 岔 —— 一口竖井，两个侧口一左一右各挂一颗光点。
  //  · 岔 —— 一口深井，三个侧口一左一右一左，井底是静止敌。
  //      两道横切分别卡在第一、第二个侧口之后 —— 掉下去之前就得把整条路排完，
  //      因为每错过一个口子，下面等着你的窗口就更窄
  { name: 'w5-20-fork', ascii: `
[#####+
      :
   o:::
      :
    1:::1
      :
      :::o
      :
    2:::2
      :
      :####]
      :
      x
`, opts: { speeds: { 1: 2.4, 2: 2.4 }, phases: { 2: 0.5 } } },

  // 21 · 通 —— 精通题。长滑行段、两次横切、终点前还有一个必须拐对的路口。
  // 21 · 通 —— 终局。原来它只是"等一个窗口然后一路冲到底" ——
  //      一个决定，剩下全是执行。现在是一条折返的滑行回路：
  //      去程两道横切、回程一道，而全程只有两小段实线可以落脚。
  //      难的不是任何单独一次穿越，是**先想清楚在哪两段实线上分别停一次** ——
  // 21 · 通 —— 终局。一条折返的滑行回路：去程两道横切、回程两道，
  //      而全程只有两小段实线可以落脚。回程半路垂下一条支路通向按钮，
  //      它开的是底层那道守着出口的门 —— 支路口同样在虚线上。
  //      难的不是任何单独一次穿越，是先想清楚：在哪两段实线上各停一次、
  //      哪一趟顺手把按钮按了 —— 而这一切都得在踏进第一段虚线之前排完
  { name: 'w5-21-through', ascii: `
      1     2
[##:::::##:::::+
      1     2  :
     3     4   :
   +:::::##::::+
   : 3 :   4
   :   A
   ######a####]
`, opts: {
    buttons: { A: 'toggle' },
    speeds: { 1: 2.4, 2: 2.4, 3: 2.4, 4: 2.4 }, phases: { 2: 0.5, 3: 0.25, 4: 0.75 },
  } },

];
